<h1>とっぷぺーじ</h1>

<!-- Tweet Box -->
<%= form_for Tweet.new do |f| %>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <%= f.submit %>
<% end %>

<!-- Time Line -->
<% @tweets.reverse_each do |tweet| %>
  <h1><%= tweet.title %></h1>
  <p><%= tweet.content %></p>
<% end %>

