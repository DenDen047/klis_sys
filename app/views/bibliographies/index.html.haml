%h1 Listing bibliographies

%table
  %thead
    %tr
      %th Name
      %th Url
      %th Report
      %th
      %th
      %th

  %tbody
    - @bibliographies.each do |bibliography|
      %tr
        %td= bibliography.name
        %td= bibliography.url
        %td= bibliography.report_id
        %td= link_to 'Show', bibliography
        %td= link_to 'Edit', edit_bibliography_path(bibliography)
        %td= link_to 'Destroy', bibliography, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Bibliography', new_bibliography_path

